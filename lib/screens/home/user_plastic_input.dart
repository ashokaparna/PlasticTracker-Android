import 'package:flutter/material.dart';

class PlasticInputCategories extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.blue.withOpacity(0.9),
      appBar: AppBar(),
      body: Container(
        child: Text('plastic  categories'),
      ),
    );
  }
}
